{
  "name": "phishing-detection-app",
  "version": "1.0.0",
  "description": "A beginner-friendly phishing detection web application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "concurrently \"node server.js\" \"cd client && npx react-scripts start\"",
    "client:install": "cd client && npm install",
    "install:all": "npm install && cd client && npm install",
    "build": "cd client && npm run build",
    "postinstall": "cd client && npm install"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "better-sqlite3": "^9.4.3",
    "uuid": "^9.0.0",
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
